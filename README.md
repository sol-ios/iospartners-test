# IOS Partners website

Static site with a plain assets/ folder (no bundling). Upload every file to the repository root, keep assets/ as a subfolder, enable GitHub Pages (Settings > Pages > deploy from branch, root). Keep .nojekyll.
