# IOS Partners website

Static site. Open `index.html`, or deploy the folder as-is (GitHub Pages: Settings > Pages > deploy from branch, root).

- All imagery is WebP, resized and compressed
- Images below the fold lazy-load
- Canvas maps pause when off-screen or when the tab is inactive
- Maps initialise only when their section approaches the viewport
