# IOS Partners website

Static site. Upload every file to the repository root, keeping `assets/` as a subfolder, then enable GitHub Pages (Settings > Pages > deploy from a branch, root).

`index.html` is the home page. `site.js` carries all scripts plus the logos and team photos. Keep `.nojekyll` — without it GitHub Pages filters some files.
