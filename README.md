# IOS Partners website

Self-contained static site. Every page is a single HTML file with all images, fonts, styles and scripts embedded — no assets folder, no build step.

Deploy: push these files to the repo root and enable GitHub Pages (Settings > Pages > deploy from branch, root). `index.html` is the home page.
